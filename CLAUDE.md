## 言語設定

このリポジトリは日本人向け日本語専用プロダクトのため、グローバルルールの言語設定を以下のように上書きする。

- コミットメッセージ：日本語
- README.md：日本語のみ

## バージョン管理

- バージョン記載箇所：`manifest.json` の `version`
- 更新のたびに semver に従ってバージョンを上げること
- README.md の 更新履歴 セクションに変更内容を追記すること
  - 形式は [Keep a Changelog](https://keepachangelog.com/) に準拠すること（`[Unreleased]` セクションは使わない）
  - バージョン見出し：`### [1.0.2] - 2026-05-28`
  - 変更種別を `####` 見出しで分類すること
    - `追加` / `変更` / `非推奨` / `削除` / `修正` / `セキュリティ`

